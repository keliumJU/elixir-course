#Introduction
#Elixir: functional programming language used to build distributed and scalable applications, compiles under the erlang virtual machine.

#Interactive mode iex, but instead i will write everything that the interactive mode shows with an IO.puts


IO.puts("sum two numbers: ")
IO.puts(40+2)

IO.puts("concat two strings: ")
IO.puts("hello"<>" world")

IO.puts("example script")
IO.puts("Hello world from Elixir")
